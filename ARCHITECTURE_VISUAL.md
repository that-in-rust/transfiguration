# Architecture Visualization Guide

## System Architecture Layers

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE LAYER                                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                    │
│  │    CLI      │    │ LSP Server  │    │  Web UI     │                    │
│  │   (cargo)   │    │ (IDE integ) │    │ (explore)   │                    │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘                    │
│         └────────────────────┼──────────────────┘                           │
└─────────────────────────────┼────────────────────────────────────────────────┘
                              │
┌─────────────────────────────┼────────────────────────────────────────────────┐
│                    COMPILATION ORCHESTRATOR                                  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────┐            │
│  │                      Salsa Database                         │            │
│  │  (Incremental Computation Engine)                           │            │
│  │                                                              │            │
│  │  Queries:                                                    │            │
│  │  • parse_file(path) → AST                                   │            │
│  │  • build_cpg(ast) → CPG                                     │            │
│  │  • analyze_deps(cpg) → DepGraph                             │            │
│  │  • compute_hash(func) → Fingerprint                         │            │
│  │  • check_cache(func) → Option<Artifact>                     │            │
│  │  • compile_func(func) → Artifact                            │            │
│  │                                                              │            │
│  │  [Automatic Memoization + Dependency Tracking]              │            │
│  └────────────────────────────────────────────────────────────┘            │
└─────────────────────────────┬────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼────────┐   ┌────────▼─────────┐   ┌──────▼────────┐
│   FRONTEND     │   │   ANALYSIS       │   │   BACKEND     │
│     LAYER      │   │     LAYER        │   │     LAYER     │
└───────┬────────┘   └────────┬─────────┘   └──────┬────────┘
        │                     │                     │
┌───────▼──────────────────────────────────────────────────────────────────────┐
│                         DATA PERSISTENCE LAYER                               │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────┐            │
│  │                       CozoDB                                │            │
│  │                 (Graph Database)                            │            │
│  │                                                              │            │
│  │  Relations:                                                  │            │
│  │  ┌─────────────────┐  ┌─────────────────┐                 │            │
│  │  │   CPG Schema    │  │  Cache Schema   │                 │            │
│  │  │                 │  │                 │                 │            │
│  │  │ • ast_nodes     │  │ • fingerprints  │                 │            │
│  │  │ • functions     │  │ • artifacts     │                 │            │
│  │  │ • control_flow  │  │ • build_session │                 │            │
│  │  │ • data_flow     │  │                 │                 │            │
│  │  │ • call_graph    │  │                 │                 │            │
│  │  │ • dependencies  │  │                 │                 │            │
│  │  └─────────────────┘  └─────────────────┘                 │            │
│  │                                                              │            │
│  │  Datalog Queries for Analysis & Invalidation                │            │
│  └────────────────────────────────────────────────────────────┘            │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## Frontend Layer (Parsing)

```
Input Files (*.rs, *.c, *.cpp)
        │
        ▼
┌─────────────────────┐
│   File Watcher      │
│   (inotify/FSEvents)│
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────────────────────┐
│          Parser Selection                            │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │  .rs     │  │  .c      │  │  .cpp    │         │
│  │  files   │  │  files   │  │  files   │         │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘         │
│       │             │             │                │
│       ▼             ▼             ▼                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │tree-sitter│ │tree-sitter│ │tree-sitter│        │
│  │  -rust   │  │   -c     │  │  -cpp    │         │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘         │
│       │             │             │                │
│       └─────────────┴─────────────┘                │
│                     │                              │
│                     ▼                              │
│          ┌──────────────────────┐                 │
│          │  Incremental Parse   │                 │
│          │  (reuse old tree)    │                 │
│          └──────────┬───────────┘                 │
└─────────────────────┼──────────────────────────────┘
                      │
                      ▼
              ┌──────────────┐
              │     AST      │
              │  (Concrete   │
              │  Syntax Tree)│
              └──────┬───────┘
                      │
                      ▼
              ┌──────────────┐
              │   Semantic   │
              │   Analysis   │
              └──────┬───────┘
                      │
                      ▼
                 To CPG Builder
```

**Repos Used**:
- tree-sitter (parser framework)
- tree-sitter-{rust,c,cpp} (grammars)
- syn (Rust semantic analysis)

---

## CPG Builder (Analysis Layer)

```
                        AST
                         │
                         ▼
        ┌────────────────────────────────┐
        │     CPG Generator              │
        │   (Joern-inspired)             │
        └────────┬───────────────────────┘
                 │
    ┏━━━━━━━━━━━━┻━━━━━━━━━━━━┓
    ▼            ▼             ▼
┌────────┐  ┌────────┐  ┌──────────┐
│  AST   │  │  CFG   │  │   DFG    │
│ Nodes  │  │Builder │  │ Builder  │
└────┬───┘  └────┬───┘  └────┬─────┘
     │           │           │
     └───────────┴───────────┘
                 │
                 ▼
        ┌─────────────────┐
        │  Unified CPG    │
        │                 │
        │  • Functions    │
        │  • Basic Blocks │
        │  • Variables    │
        │  • Edges:       │
        │    - CALLS      │
        │    - CFG        │
        │    - DEF-USE    │
        │    - DEPENDS    │
        └────────┬────────┘
                 │
                 ▼
        ┌─────────────────┐
        │  Dependency     │
        │  Extractor      │
        │                 │
        │  For each func: │
        │  • Direct calls │
        │  • Data deps    │
        │  • Type deps    │
        │  • Macros used  │
        └────────┬────────┘
                 │
                 ▼
            Store in CozoDB
```

**Repos Used**:
- joern (CPG design reference)
- codepropertygraph (schema)
- petgraph (graph algorithms)

---

## CozoDB Schema Design

```
┌─────────────────────────────────────────────────────────────┐
│                    CozoDB Relations                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────┐
│       CPG Relations         │
├─────────────────────────────┤
│                             │
│  file                       │
│  ├─ id: UUID               │
│  ├─ path: String           │
│  └─ content_hash: String   │
│                             │
│  ast_node                   │
│  ├─ id: UUID               │
│  ├─ type: String           │
│  ├─ parent_id: UUID?       │
│  └─ file_id: UUID          │
│                             │
│  function                   │
│  ├─ id: UUID               │
│  ├─ name: String           │
│  ├─ signature: String      │
│  ├─ file_id: UUID          │
│  └─ content_hash: String   │
│                             │
│  control_flow_edge          │
│  ├─ from_block: UUID       │
│  ├─ to_block: UUID         │
│  └─ condition: String?     │
│                             │
│  data_flow_edge             │
│  ├─ from_node: UUID        │
│  ├─ to_node: UUID          │
│  └─ variable: String       │
│                             │
│  dependency                 │
│  ├─ from_func: UUID        │
│  ├─ to_func: UUID          │
│  ├─ dep_type: String       │
│  └─ strength: String       │
└─────────────────────────────┘

┌─────────────────────────────┐
│     Cache Relations         │
├─────────────────────────────┤
│                             │
│  fingerprint                │
│  ├─ function_id: UUID      │
│  ├─ content_hash: String   │
│  ├─ deps_hash: String      │
│  └─ transitive_hash: String│
│                             │
│  compilation_artifact       │
│  ├─ function_id: UUID      │
│  ├─ llvm_ir: String        │
│  ├─ object_code: Bytes     │
│  └─ timestamp: Int         │
│                             │
│  build_session              │
│  ├─ id: UUID               │
│  ├─ timestamp: Int         │
│  ├─ functions_recompiled   │
│  └─ cache_hit_rate: Float  │
└─────────────────────────────┘

Sample Query (Transitive Dependencies):

  transitive[x, y] := *dependency{from_func: x, to_func: y}
  transitive[x, z] := transitive[x, y], *dependency{from_func: y, to_func: z}

  ?[affected] :=
      *function{name: "changed_func", id: changed_id},
      transitive[affected, changed_id]
```

---

## Incremental Compilation Flow

```
┌────────────────────────────────────────────────────────────────┐
│  1. Change Detection                                           │
│                                                                │
│  File modified: src/foo.rs                                     │
│       │                                                         │
│       ▼                                                         │
│  [inotify event] → Salsa invalidation                          │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  2. Incremental Parse                                          │
│                                                                │
│  tree-sitter:                                                  │
│    parse_incremental(new_content, old_tree)                   │
│       │                                                         │
│       ▼                                                         │
│    New AST (only changed subtrees re-parsed)                   │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  3. CPG Update (in CozoDB)                                     │
│                                                                │
│  • Delete old CPG nodes for changed functions                  │
│  • Insert new CPG nodes                                        │
│  • Update edges (call graph, data flow)                        │
│       │                                                         │
│       ▼                                                         │
│  Changed functions: [foo(), bar()]                             │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  4. Affected Function Query (CozoDB Datalog)                   │
│                                                                │
│  ?[affected] :=                                                │
│      changed_func[changed],                                    │
│      transitive[affected, changed]                             │
│       │                                                         │
│       ▼                                                         │
│  Affected: [foo(), bar(), baz(), main()]                       │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  5. Fingerprint Computation (BLAKE3)                           │
│                                                                │
│  For each affected function:                                   │
│    hash = BLAKE3(                                              │
│      content_hash(function) ||                                 │
│      sorted([fingerprint(dep) for dep in dependencies])        │
│    )                                                            │
│       │                                                         │
│       ▼                                                         │
│  foo: hash_123  (CHANGED)                                      │
│  bar: hash_456  (UNCHANGED - cache hit!)                       │
│  baz: hash_789  (CHANGED - dep on foo)                         │
│  main: hash_abc (CHANGED - dep on baz)                         │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  6. Cache Check (CozoDB)                                       │
│                                                                │
│  For each function with changed hash:                          │
│    ?[artifact] :=                                              │
│        *fingerprint{function_id: $func, transitive_hash: $hash}│
│        *compilation_artifact{function_id: $func}               │
│       │                                                         │
│       ▼                                                         │
│  Cache miss: [foo, baz, main]  ← Need to recompile            │
│  Cache hit:  [bar]              ← Reuse artifact               │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  7. Parallel Compilation (Rayon)                               │
│                                                                │
│  functions_to_compile.par_iter()                               │
│    .map(|func| {                                               │
│        compile_function(func)  // Salsa query                  │
│    })                                                           │
│       │                                                         │
│       ▼                                                         │
│  Thread 1: Compile foo() → foo.o                               │
│  Thread 2: Compile baz() → baz.o                               │
│  Thread 3: Compile main() → main.o                             │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  8. LLVM IR Generation (inkwell)                               │
│                                                                │
│  For each function:                                            │
│    1. Build LLVM context                                       │
│    2. Generate IR from CPG                                     │
│    3. Run optimization passes                                  │
│    4. Emit object code                                         │
│       │                                                         │
│       ▼                                                         │
│  foo.o, baz.o, main.o + [cached: bar.o]                        │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  9. Store Artifacts (CozoDB)                                   │
│                                                                │
│  For each newly compiled function:                             │
│    INSERT INTO compilation_artifact (                          │
│      function_id, llvm_ir, object_code, timestamp              │
│    )                                                            │
│    INSERT INTO fingerprint (                                   │
│      function_id, transitive_hash, ...                         │
│    )                                                            │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  10. Incremental Linking (mold)                                │
│                                                                │
│  mold --incremental                                            │
│    foo.o bar.o baz.o main.o → binary                           │
│       │                                                         │
│       ▼                                                         │
│  ✅ Binary updated!                                            │
└────────┬───────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│  11. Statistics Update (CozoDB)                                │
│                                                                │
│  INSERT INTO build_session (                                   │
│    timestamp: now(),                                           │
│    functions_recompiled: [foo, baz, main],                     │
│    total_functions: 1000,                                      │
│    cache_hit_rate: 99.7%,                                      │
│    build_time_ms: 150                                          │
│  )                                                              │
│       │                                                         │
│       ▼                                                         │
│  📊 Report: 3/1000 functions recompiled (0.3%)                 │
│             Build time: 150ms (vs 30s clean build)             │
│             Speedup: 200x                                      │
└────────────────────────────────────────────────────────────────┘
```

---

## Dependency Graph Example

```
Source Files:
  main.rs       foo.rs        bar.rs
    │             │             │
    ▼             ▼             ▼
  main()        foo()         bar()
   │             │             │
   └──calls──────┤             │
                 └──calls──────┘

Dependency Graph in CozoDB:

  ┌────────┐
  │ main() │
  └────┬───┘
       │ calls
       ▼
  ┌────────┐
  │  foo() │
  └────┬───┘
       │ calls
       ▼
  ┌────────┐
  │  bar() │
  └────────┘

If bar() changes:
  1. Recompile bar()
  2. Query: ?[x] := transitive[x, bar_id]
  3. Result: {foo, main}
  4. Recompile foo() (depends on bar)
  5. Recompile main() (depends on foo)

Transitive Hash:
  bar_hash  = BLAKE3(bar_content)
  foo_hash  = BLAKE3(foo_content || bar_hash)
  main_hash = BLAKE3(main_content || foo_hash)

If only main() changes:
  - main_hash changes
  - foo_hash UNCHANGED (doesn't depend on main)
  - bar_hash UNCHANGED (doesn't depend on main)
  → Only recompile main() ✅
```

---

## Multi-Language Integration

```
Project Structure:
  project/
    ├── src/
    │   ├── main.rs      (Rust)
    │   ├── utils.c      (C)
    │   └── algo.cpp     (C++)
    └── include/
        └── ffi.h        (C header)

Unified CPG:

┌────────────────────────────────────────────┐
│          CozoDB Graph Database             │
├────────────────────────────────────────────┤
│                                            │
│  Rust Functions:                           │
│  ┌──────────┐                              │
│  │ main()   │─────calls────┐               │
│  └──────────┘              │               │
│                            ▼               │
│  C Functions:         ┌──────────┐         │
│  ┌──────────┐        │  ffi     │         │
│  │ c_util() │◄───────│  bridge  │         │
│  └──────────┘        └──────────┘         │
│       │                                    │
│       │ calls                              │
│       ▼                                    │
│  C++ Functions:                            │
│  ┌───────────┐                             │
│  │ algo()    │                             │
│  └───────────┘                             │
│                                            │
│  All in same graph!                        │
│  Cross-language dependency tracking        │
└────────────────────────────────────────────┘

Benefits:
  ✅ Track Rust → C FFI calls
  ✅ Detect when C change affects Rust
  ✅ Unified optimization (inline across languages)
  ✅ Single cache for entire project
```

---

## Distributed Caching Architecture

```
                   Developer Machines
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
   ┌─────────┐      ┌─────────┐      ┌─────────┐
   │  Dev 1  │      │  Dev 2  │      │  Dev 3  │
   │         │      │         │      │         │
   │ Local   │      │ Local   │      │ Local   │
   │ CozoDB  │      │ CozoDB  │      │ CozoDB  │
   └────┬────┘      └────┬────┘      └────┬────┘
        │                │                │
        │ upload/download artifacts      │
        │                │                │
        └────────────────┼────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────┐
        │   Remote Cache (S3/Redis)       │
        │                                 │
        │  Key: BLAKE3(transitive_hash)   │
        │  Value: {                       │
        │    llvm_ir: "...",              │
        │    object_code: bytes,          │
        │    metadata: {...}              │
        │  }                              │
        │                                 │
        │  Content-Addressed Storage      │
        │  (Same hash = same artifact)    │
        └─────────────────────────────────┘
                         ▲
                         │
                         ▼
        ┌─────────────────────────────────┐
        │         CI/CD Server            │
        │                                 │
        │  • Builds on clean machine      │
        │  • Uploads all artifacts        │
        │  • Developers get instant cache │
        └─────────────────────────────────┘

Cache Hit Flow:
  1. Dev changes function foo()
  2. Compute transitive_hash
  3. Query remote cache: GET /cache/{hash}
  4. If found → Download artifact (50-200ms)
  5. If not found → Compile & upload

Benefits:
  ✅ Team shares compilation work
  ✅ New developers get instant builds
  ✅ CI builds populate cache
  ✅ Works across branches (same code = same hash)
```

---

## Performance Characteristics

```
┌──────────────────────────────────────────────────────────────┐
│                     Build Performance                         │
└──────────────────────────────────────────────────────────────┘

Clean Build (1000 functions):
  ┌─────────────────────────────────────────┐
  │ Parse:        5s   ██████               │
  │ CPG Build:   10s   ████████████         │
  │ Analysis:     3s   ████                 │
  │ Compile:     80s   ████████████████████ │
  │ Link:         2s   ██                   │
  │ Total:      100s                        │
  └─────────────────────────────────────────┘

Incremental Build (1% changed = 10 functions):
  ┌─────────────────────────────────────────┐
  │ Change detect: 0.01s ▌                  │
  │ Parse:         0.05s ▌                  │
  │ CPG update:    0.10s ▌                  │
  │ Query deps:    0.10s ▌                  │
  │ Fingerprint:   0.05s ▌                  │
  │ Compile (10):  0.80s ████               │
  │ Link:          0.10s ▌                  │
  │ Total:         1.21s                    │
  │ Speedup:        82x                     │
  └─────────────────────────────────────────┘

Cache Hit (remote):
  ┌─────────────────────────────────────────┐
  │ Query:       0.001s ▌                   │
  │ Download:    0.150s ██                  │
  │ Total:       0.151s                     │
  │ Speedup:      662x                      │
  └─────────────────────────────────────────┘

Cache Hit (local):
  ┌─────────────────────────────────────────┐
  │ Query:       0.001s ▌                   │
  │ Load:        0.005s ▌                   │
  │ Total:       0.006s                     │
  │ Speedup:   16,667x                      │
  └─────────────────────────────────────────┘
```

---

## Component Repository Mapping

```
System Component → Repository Used

┌─────────────────────────────────────────────┐
│ Frontend (Parsing)                          │
├─────────────────────────────────────────────┤
│ • Rust Parser    → tree-sitter-rust, syn   │
│ • C Parser       → tree-sitter-c           │
│ • C++ Parser     → tree-sitter-cpp         │
│ • Framework      → tree-sitter             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ CPG & Analysis                              │
├─────────────────────────────────────────────┤
│ • CPG Design     → joern, codepropertygraph│
│ • Graph Algos    → petgraph                │
│ • Optimizations  → egg (e-graphs)          │
│ • Data Flow      → joern (reference)       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Incremental Engine                          │
├─────────────────────────────────────────────┤
│ • Framework      → salsa                   │
│ • Reference      → rust-analyzer           │
│ • Hashing        → BLAKE3, rustc-hash      │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Graph Database                              │
├─────────────────────────────────────────────┤
│ • Primary DB     → CozoDB                  │
│ • Datalog Ref    → souffle                 │
│ • Visualization  → graphviz                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Backend (Code Generation)                   │
├─────────────────────────────────────────────┤
│ • LLVM IR        → inkwell                 │
│ • LLVM Core      → llvm-project            │
│ • Alternative    → cranelift               │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Linking                                     │
├─────────────────────────────────────────────┤
│ • Fast Linker    → mold                    │
│ • LLD Ref        → llvm-project/lld        │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Build System & Caching                      │
├─────────────────────────────────────────────┤
│ • Rust Build     → cargo                   │
│ • Modern Build   → buck2, bazel            │
│ • Caching        → sccache, ccache         │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Parallelism & Utilities                     │
├─────────────────────────────────────────────┤
│ • Parallel       → rayon                   │
│ • Hashing        → BLAKE3                  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Reference Implementations                   │
├─────────────────────────────────────────────┤
│ • Incremental    → rust-analyzer, salsa    │
│ • Fast Compiler  → swc                     │
│ • JIT            → hhvm                    │
│ • Type System    → chalk                   │
│ • Interpreter    → miri, RustPython        │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Transformation & Analysis                   │
├─────────────────────────────────────────────┤
│ • Transpiler     → c2rust                  │
│ • Diff Tool      → difftastic              │
│ • Static Anal    → static-analysis (list)  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Documentation                               │
├─────────────────────────────────────────────┤
│ • Compiler Dev   → rustc-dev-guide         │
└─────────────────────────────────────────────┘
```

---

## Technology Decision Tree

```
Question: What to use for...?

Parsing source code?
  └─> tree-sitter (incremental, multi-language)
      OR syn (Rust-specific, more semantic info)

Storing CPG?
  └─> CozoDB (graph DB with Datalog queries)

Incremental computation?
  └─> Salsa (proven in rust-analyzer)

Hashing artifacts?
  └─> BLAKE3 (fastest, parallelizable)

Code generation?
  └─> inkwell + LLVM (mature, best optimization)
      OR cranelift (faster compile, less optimized)

Linking?
  └─> mold (10-50x faster than ld)

Graph algorithms?
  └─> petgraph (Rust-native, good API)

Optimizations?
  └─> egg (e-graphs, state-of-art)

Parallelism?
  └─> rayon (work-stealing, composable)

Distributed cache?
  └─> Design inspired by sccache + Buck2

Build system?
  └─> Custom (using Salsa) + cargo for Rust-specific

Multi-language parsing?
  └─> tree-sitter (50+ grammars available)
```

---

## Memory Layout

```
┌─────────────────────────────────────────────────────────────┐
│                    Memory Usage                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────┐               │
│  │  Salsa Database (in-memory)             │               │
│  │  • Parsed ASTs (memoized)      ~500MB   │               │
│  │  • Query results (cached)      ~200MB   │               │
│  │  • Dependency graph            ~100MB   │               │
│  │  • LRU eviction after 1 hour            │               │
│  └─────────────────────────────────────────┘               │
│                        │                                    │
│                        ▼                                    │
│  ┌─────────────────────────────────────────┐               │
│  │  CozoDB (on-disk + cache)               │               │
│  │  • Memory-mapped DB           ~200MB    │               │
│  │  • Query cache                ~100MB    │               │
│  │  • Disk: CPG + artifacts      ~5GB      │               │
│  └─────────────────────────────────────────┘               │
│                        │                                    │
│                        ▼                                    │
│  ┌─────────────────────────────────────────┐               │
│  │  LLVM Context (per thread)              │               │
│  │  • IR builder                 ~50MB     │               │
│  │  • Optimization state         ~100MB    │               │
│  │  • 8 threads = 1.2GB total              │               │
│  └─────────────────────────────────────────┘               │
│                                                             │
│  Total: ~2.3GB for 100k LOC project                         │
│  Target: <4GB for production use                            │
└─────────────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
Compilation Error:
  ┌─────────────────────┐
  │ Parse Error         │
  └──────┬──────────────┘
         │
         ▼
  ┌─────────────────────┐
  │ Error Recovery      │
  │ (tree-sitter)       │
  └──────┬──────────────┘
         │
         ▼
  ┌─────────────────────┐
  │ Generate Error Node │
  │ in CPG              │
  └──────┬──────────────┘
         │
         ▼
  ┌─────────────────────┐
  │ Store in CozoDB     │
  │ with span info      │
  └──────┬──────────────┘
         │
         ▼
  ┌─────────────────────┐
  │ Format Error        │
  │ Message with        │
  │ context from CPG    │
  └──────┬──────────────┘
         │
         ▼
  Display to user:

  error[E0308]: mismatched types
    --> src/main.rs:10:5
     |
  10 |     foo(42)
     |     ^^^^^^^ expected `String`, found `i32`
     |
  help: call `.to_string()` on the integer

  Suggested by: data flow analysis in CPG
```

---

This architecture leverages the best practices from 39 reference repositories to build a next-generation incremental compiler with graph-based dependency tracking and function-level caching.

**Key Innovation**: Graph database (CozoDB) + Incremental computation (Salsa) + Content addressing (BLAKE3) = Fast, correct, scalable compilation.
